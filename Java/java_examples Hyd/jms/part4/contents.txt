Shows how to specify DeliveryMode.

default is PERSISTENT.
Using the NON_PERSISTENT delivery mode may improve performance and reduce storage overhead.

To run this example we need to configure persistant store.